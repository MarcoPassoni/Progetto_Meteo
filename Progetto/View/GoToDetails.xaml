<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Progetto.View.GoToDetails"
             xmlns:ViewModel="clr-namespace:Progetto.ModelView"
             xmlns:Model="clr-namespace:Progetto.Model"
             x:DataType="ViewModel:ModelViewDetails"
             Title="GoToDetails">

    <VerticalStackLayout>
        <Label Text="{Binding Location.Name, StringFormat='Città: {0}'}" TextColor="White"/>
        <Label Text="{Binding Current.Temperature, StringFormat='Temperatura: {0}'}" TextColor="White"/>
        <Label Text="{Binding Weather, StringFormat='Tempo: {0}'}" TextColor="White"/>
        <Button Text="togli dai preferiti" Command="{Binding RemoveInPreferencesCommand}"/>
        <Button Text="guarda tempo settimana" Command="{Binding }"/>
    </VerticalStackLayout>
</ContentPage>
    